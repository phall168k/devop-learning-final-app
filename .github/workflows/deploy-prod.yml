name: Deploy on main branch

on:
    push:
        branches:
            - main 

env:
    IMAGE_NAME: phall168k/devop-learning-final-app

jobs:
    build-push-deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v6

            - name: Login to Docker Hub
              uses: docker/login-action@v3
              with:
                username: ${{ secrets.DOCKERHUB_USERNAME }}
                password: ${{ secrets.DOCKERHUB_TOKEN }}
            
            - name: Extract SHA
              id: vars
              run: ech "sha=$(echo ${{ github.sha }} | cut -ci-7)" >> $GITHUB_OUTPUT

            - name: Build and push Docker image
              uses: docker/build-push-action@v7
              with:
                context: .
                push: true
                tags: |
                    ${{ env.IMAGE_NAME }}:latest
                    ${{ env.IMAGE_NAME }}:sha-${{ steps.vars.outputs.sha }}
            
            # - name: Deploy to VPS via SSH
            #   uses: appleboy/ssh-action@v1
            #   with:
            #     host: ${{ secrets.SHH_PORT }}
            #     username: ${{ secrets.SSH_USER }}
            #     key: ${{ secrets.SSH_KEY }}
            #     port: ${{ secrets.SSH_PORT }}
            #     script: |
            #             cd devop-learning-final-app
            #             git reset --hard HEAD
            #             git fetch origin main
            #             git checkout main
            #             git reset --hard origin/main
            #             git clean -fd
            #             docker compose -f docker-compose.prod.yml --env-file .env.prod -p devop-learning-final-app-prod pull
            #             docker compose -f docker-compose.prod.yml --env-file .env.prod -p devop-learning-final-app-prod up -d --remove-orphans
            #             docker image prune -f

